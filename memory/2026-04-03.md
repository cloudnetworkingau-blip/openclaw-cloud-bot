# 2026-04-03

## Minimax Model Configuration

Successfully configured Minimax as a new model provider in OpenClaw today.

### What was done:
1. Added Minimax provider configuration to `~/.openclaw/openclaw.json`
2. Configured Minimax API key in auth profiles
3. Added two Minimax models: `abab6.5s-chat` and `abab6.5-chat`
4. Updated agent defaults to include `minimax/abab6.5s-chat` as fallback #6
5. Added Minimax alias configuration
6. Updated `~/.openclaw/agents/main/agent/models.json` with Minimax provider details and API key
7. Restarted OpenClaw gateway to apply changes
8. Successfully switched current session to use `minimax/abab6.5s-chat`

### Key details:
- **API Key**: Configured with provided Minimax API key (starts with `sk-cp-...`)
- **Base URL**: `https://api.minimax.chat/v1`
- **Primary model configured**: `minimax/abab6.5s-chat`
- **Alias**: "Minimax"
- **Status**: Successfully verified model is available in `openclaw models list` and can be selected for sessions

### Configuration files modified:
1. `~/.openclaw/openclaw.json` - Added Minimax provider, auth profile, and agent defaults
2. `~/.openclaw/agents/main/agent/models.json` - Added Minimax provider with API key and model definitions

The configuration was completed successfully and the model is now available for use in OpenClaw sessions.